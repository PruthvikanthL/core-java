class Camera {
    public static void main(String[] args) {
        String[] Cameras = {"DSLR Camera", "Mirrorless Camera", "Action Camera", "Instant Camera", "360-Degree Camera"};

        for (String Camera : Cameras) {
            System.out.println("Camera --> " + Camera);
        }
    }
}
